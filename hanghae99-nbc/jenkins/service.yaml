apiVersion: v1
kind: Service
metadata:
  name: jenkins-service
  namespace: devops-tools
spec:
  selector:
    app: jenkins-server
  ports:
    - name: https
      port: 8080
      targetPort: 8080
    - name: jnlp
      port: 50000
      targetPort: 50000
